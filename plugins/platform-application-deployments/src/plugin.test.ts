import { platformApplicationDeploymentsPlugin } from './plugin';

describe('platform-application-deployments', () => {
  it('should export plugin', () => {
    expect(platformApplicationDeploymentsPlugin).toBeDefined();
  });
});
